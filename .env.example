# PD2 Shift Console - Example Environment Variables
# Copy this file to .env and fill in secure values (never commit real secrets)

# Server listening port
PORT=8787

# Comma-separated list of allowed origins (exact match protocol+host+optional port)
ALLOW_ORIGINS=http://127.0.0.1,http://localhost

# Secure API token (generate with: node scripts/generate-token.js)
API_TOKEN=REPLACE_WITH_SECURE_RANDOM

# Google service account credentials (choose ONE method)
# Method 1: Path to JSON key file on disk (recommended in production)
# GOOGLE_SERVICE_ACCOUNT_FILE=C:\\secure\\service-account.json
# Method 2: Inline single-line JSON (must escape newlines in private_key)
# GOOGLE_SERVICE_ACCOUNT_JSON={"type":"service_account", ... }
# Legacy (still accepted):
# GOOGLE_CREDENTIALS_JSON={"type":"service_account", ... }

# Node environment: production | development
NODE_ENV=production

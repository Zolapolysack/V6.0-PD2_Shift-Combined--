# 📤 โมดูลอัพโหลดไฟล์รายงานผลผลิต PD2

โมดูลสำหรับอัพโหลดและประมวลผลไฟล์ Excel/CSV จากกะ A, กะ B และไฟล์เสริม เพื่อสร้างรายงานผลการผลิตแบบอัตโนมัติ

## 📁 โครงสร้างไฟล์

```
upload-module/
├── index.html          # หน้าเว็บหลัก (UI)
├── processor.js        # ตัวประมวลผลข้อมูล
└── README.md          # คู่มือการใช้งาน
```

## ✨ คุณสมบัติหลัก

### 🎯 การทำงาน
- ✅ อัพโหลดไฟล์ Excel (.xlsx, .xls) หรือ CSV
- ✅ รองรับ 3 ไฟล์: กะ A (จำเป็น), กะ B (จำเป็น), ไฟล์เสริม (ไม่บังคับ)
- ✅ Drag & Drop หรือคลิกเพื่ออัพโหลด
- ✅ ประมวลผลอัตโนมัติ
- ✅ สร้าง Excel Report พร้อมดาวน์โหลด

### 🎨 ออกแบบ
- ใช้ Tailwind CSS 3.4.10
- Glass morphism style (ตาม base ระบบหลัก)
- Responsive design
- ไม่มี animation libraries ภายนอก (CSS/JS ล้วน)
- Dark theme สวยงาม

### ⚡ ฟีเจอร์
- Real-time progress tracking
- ตรวจสอบรูปแบบไฟล์อัตโนมัติ
- แสดงสถิติผลการประมวลผล
- Error handling ครอบคลุม
- แจ้งเตือนด้วย PD2Notify (ใช้ร่วมกับระบบหลัก)

## 🚀 การใช้งาน

### 1. เปิดหน้าเว็บ
เปิดไฟล์ `index.html` ในเบราว์เซอร์ หรือเรียกผ่าน web server

### 2. อัพโหลดไฟล์
- **กะ A**: คลิกหรือลากไฟล์กะ A มาที่ช่องสีเขียว (จำเป็น)
- **กะ B**: คลิกหรือลากไฟล์กะ B มาที่ช่องสีม่วง (จำเป็น)
- **ไฟล์เสริม**: คลิกหรือลากไฟล์ C มาที่ช่องสีเหลือง (ไม่บังคับ)

### 3. ประมวลผล
- กดปุ่ม **"เริ่มประมวลผล"** (เปิดใช้งานเมื่ออัพโหลด A และ B แล้ว)
- รอระบบประมวลผล (แสดง progress bar)
- ดูสถิติผลลัพธ์

### 4. ดาวน์โหลด
- กดปุ่ม **"ดาวน์โหลดรายงาน"**
- ไฟล์จะถูกบันทึกเป็น `PD2_Report_YYYYMMDD.xlsx`

## 📊 ข้อมูลที่ประมวลผล

### รองรับคอลัมน์ (กะ A/B):
- เวลาบันทึก
- ลำดับชุดข้อมูลที่
- วันที่
- เครื่องทอ NO
- พนักงานทอ กะ A/B
- ขนาดหน้าผ้า (รหัส)
- Lot.No
- เลขที่ใบสั่งผลิต (สร้างอัตโนมัติ)
- ยอดทอ กะ A/B
- ความเร็วรอบ กะ A/B
- ประสิทธิภาพ กะ A/B
- มิเตอร์เริ่มงาน/เลิกงาน
- ความยาวตัดม้วน

### ไฟล์เสริม (C):
- ข้อมูลตัดม้วนครั้งที่ 2 และ 3
- รหัสผ้า, Lot.No
- ความยาวตัดม้วน

## 🔧 การตั้งค่า

### เลขที่ใบสั่งผลิต
ระบบจะสร้างเลขที่ใบสั่งผลิตอัตโนมัติตามรูปแบบ:
```
YYMM + รหัส 2 หลัก
```

ตัวอย่าง: `680101` = พ.ศ. 68, เดือน 01, รหัสผ้า 01

### Code Mapping
ระบบรองรับรหัสผ้า 43 รหัส:
- 1625800 → 01
- 1825800 → 02
- 1825800SM → 03
- ... (ดูเพิ่มเติมใน processor.js)

## 🔗 การรวมเข้าระบบหลัก

### วิธีที่ 1: แยกเป็น Module
```html
<!-- ใน index.html หลัก -->
<a href="./upload-module/index.html" class="btn">
    📤 อัพโหลดไฟล์รายงาน
</a>
```

### วิธีที่ 2: รวมใน Navigation
```javascript
// ใน index.html หลัก เพิ่มปุ่มใน navigation
const btnUpload = document.getElementById('btnUpload');
btnUpload.addEventListener('click', () => {
    navigateTo('UPLOAD');
});
```

แล้วเพิ่มใน pathFor():
```javascript
function pathFor(target) {
    if (target === 'UPLOAD') return './upload-module/index.html';
    // ... existing code
}
```

## 📦 Dependencies

- **Tailwind CSS 3.4.10** - Styling framework
- **SheetJS (xlsx)** - Excel file processing
- **Google Fonts (Kanit)** - Typography
- **pd2-notify.js** - Notification system (จากระบบหลัก)

## 🎯 Browser Support

- Chrome 90+ ✅
- Firefox 88+ ✅
- Safari 14+ ✅
- Edge 90+ ✅

## 📝 หมายเหตุ

### ความแตกต่างจาก ref.py:
- ✅ Web-based (ไม่ต้องใช้ Google Colab)
- ✅ ไม่ต้องติดตั้ง Python
- ✅ ใช้งานได้ทันทีในเบราว์เซอร์
- ✅ UI สวยงามเหมาะกับระบบหลัก
- ⚠️ ฟีเจอร์บางส่วนจาก Python อาจยังไม่ครบ (เช่น complex formatting, สูตร Excel)

### การพัฒนาต่อ:
1. เพิ่ม Advanced Excel formatting
2. เพิ่มสูตรคำนวณแบบ Python script
3. รองรับ validation rules เพิ่มเติม
4. Export เป็น PDF
5. ส่งข้อมูลไป Google Sheets API

## 🐛 Troubleshooting

### ไฟล์ไม่อัพโหลด
- ตรวจสอบว่าเป็นไฟล์ .xlsx, .xls หรือ .csv
- ตรวจสอบขนาดไฟล์ไม่เกิน 10MB

### ประมวลผลล้มเหลว
- ตรวจสอบว่าไฟล์มีหัวตาราง "เครื่องทอ NO"
- ตรวจสอบรูปแบบข้อมูลในไฟล์ถูกต้อง

### รายงานไม่ครบ
- ตรวจสอบว่าอัพโหลดทั้งกะ A และ B
- ตรวจสอบ Console สำหรับ error messages

## 📞 Support

หากพบปัญหาหรือต้องการความช่วยเหลือ:
1. ตรวจสอบ Console (F12) สำหรับ error messages
2. ตรวจสอบไฟล์ให้ตรงตามรูปแบบที่กำหนด
3. ลองใหม่ด้วยปุ่ม "ประมวลผลใหม่"

---

**Version:** 1.0.0  
**Last Updated:** January 2026  
**Compatible with:** PD2 System V5.03

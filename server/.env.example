# Server
PORT=8787
ALLOW_ORIGINS=http://127.0.0.1,http://localhost
API_TOKEN=change-me-dev-token

# Google Credentials (choose one approach later)
# GOOGLE_SERVICE_ACCOUNT_JSON='{"type":"service_account", ...}'
# or
# GOOGLE_CREDENTIALS_JSON='{"installed": { ... }}'

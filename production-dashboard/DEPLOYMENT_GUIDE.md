# 🚀 Production Dashboard - Deployment Guide

## 📋 สารบัญ
1. [เตรียมความพร้อม](#1-เตรียมความพร้อม)
2. [Setup Google Sheets](#2-setup-google-sheets)
3. [Deploy Apps Script](#3-deploy-apps-script)
4. [Configure Dashboard](#4-configure-dashboard)
5. [Testing](#5-testing)
6. [Troubleshooting](#6-troubleshooting)

---

## 1️⃣ เตรียมความพร้อม

### ✅ สิ่งที่ต้องมี:
- [ ] Google Account
- [ ] Google Sheets สำหรับกะ A และ กะ B
- [ ] ข้อมูลทดสอบในแต่ละ Sheet
- [ ] Web browser (Chrome แนะนำ)

---

## 2️⃣ Setup Google Sheets

### ขั้นตอนที่ 1: สร้าง/เตรียม Google Sheets

#### **Shift A Sheet:**
1. เปิด Google Sheets สำหรับกะ A
2. ตรวจสอบโครงสร้างข้อมูล:
   ```
   | A: วันที่    | B: รหัสผ้า  | C: ยอดทอ | D: ยอดตัดม้วน |
   |--------------|-------------|----------|---------------|
   | 2026-02-09   | PD2-101     | 150.50   | 145.00        |
   ```

3. **สำคัญ!** ตรวจสอบชื่อ Tab/Sheet:
   - ถ้าชื่อไม่ใช่ **"กะ A"** ให้เปลี่ยนใน `Code.gs` → `CONFIG.SHEET_A_NAME`

4. Copy Sheet ID จาก URL:
   ```
   https://docs.google.com/spreadsheets/d/[THIS_IS_SHEET_ID]/edit
                                          ^^^^^^^^^^^^^^^^^^^^
   ```

#### **Shift B Sheet:**
ทำเหมือนกับ Shift A แต่ใช้ Sheet สำหรับกะ B

### ขั้นตอนที่ 2: ตั้งค่า Permissions

1. ไปที่ Google Sheets
2. คลิก **Share** (มุมขวาบน)
3. เลือก **Anyone with the link can view** (หรือระบุ email ของ Google Account ที่จะรัน Apps Script)

---

## 3️⃣ Deploy Apps Script

### ขั้นตอนที่ 1: สร้าง Apps Script Project

1. เปิด [Google Apps Script](https://script.google.com/)
2. คลิก **New Project**
3. ตั้งชื่อ: "Production Dashboard API"

### ขั้นตอนที่ 2: Copy Code

1. ลบโค้ดเดิมใน `Code.gs` ทั้งหมด
2. Copy โค้ดจาก `production-dashboard/Code.gs` ทั้งหมด
3. วาง (Paste) ลงใน Apps Script Editor

### ขั้นตอนที่ 3: แก้ไข Configuration

**⚠️ สำคัญมาก! แก้ไขบรรทัดนี้:**

```javascript
const CONFIG = {
  // TODO: แก้ไขเป็น Sheet ID ของคุณ
  SHEET_A_ID: 'YOUR_SHIFT_A_SHEET_ID_HERE',
  SHEET_B_ID: 'YOUR_SHIFT_B_SHEET_ID_HERE',
  
  // ตรวจสอบชื่อ Sheet/Tab
  SHEET_A_NAME: 'กะ A',
  SHEET_B_NAME: 'กะ B',
  
  // ถ้าโครงสร้างคอลัมน์ต่างจากด้านล่าง ให้แก้ไข
  COLUMNS: {
    DATE: 0,           // Column A: วันที่
    BRAND_CODE: 1,     // Column B: รหัสผ้า
    PRODUCTION: 2,     // Column C: ยอดทอ
    CUTTING: 3         // Column D: ยอดตัดม้วน
  },
  
  // เปิด debug mode ตอนทดสอบ
  DEBUG: true
};
```

### ขั้นตอนที่ 4: Run Setup Function

1. เลือกฟังก์ชัน **`setup`** จาก dropdown (ด้านบนโค้ด)
2. คลิก **Run** (ปุ่มเล่น ▶️)
3. **Grant Permissions:**
   - คลิก **Review permissions**
   - เลือก Google Account ของคุณ
   - คลิก **Advanced** → **Go to Production Dashboard API (unsafe)**
   - คลิก **Allow**

4. ดูผลลัพธ์ใน **Execution log** (ด้านล่าง):
   ```
   ✓ Access to Sheet A successful
   ✓ Access to Sheet B successful
   ```

### ขั้นตอนที่ 5: Test API

1. เลือกฟังก์ชัน **`testAPI`**
2. คลิก **Run**
3. ตรวจสอบ log - ต้องแสดงข้อมูลที่ดึงมาได้

### ขั้นตอนที่ 6: Deploy as Web App

1. คลิก **Deploy** → **New deployment**
2. คลิก **Select type** → เลือก **Web app**
3. กำหนดค่า:
   - **Description:** "Production Dashboard API v1"
   - **Execute as:** **Me** (email ของคุณ)
   - **Who has access:** **Anyone** ⚠️ สำคัญ!
4. คลิก **Deploy**
5. **Copy Web App URL** (มีรูปแบบ):
   ```
   https://script.google.com/macros/s/AKfycby.../exec
   ```

6. เก็บ URL นี้ไว้!

---

## 4️⃣ Configure Dashboard

### ขั้นตอนที่ 1: แก้ไข config.js

1. เปิดไฟล์ `production-dashboard/config.js`
2. แก้ไขบรรทัดนี้:
   ```javascript
   const API_URL = 'YOUR_WEB_APP_URL_HERE';
   ```
   
   เป็น:
   ```javascript
   const API_URL = 'https://script.google.com/macros/s/AKfycby.../exec';
   ```

3. Save ไฟล์

### ขั้นตอนที่ 2: ทดสอบ Dashboard

1. เปิดไฟล์ `production-dashboard/index.html` ใน browser
   - หรือใช้ local server: `python -m http.server 8000`

2. จะ redirect ไป `dashboard.html` อัตโนมัติ

3. ทดสอบ:
   - เลือกวันที่ (ที่มีข้อมูล)
   - เลือกกะ A หรือ B
   - คลิก "โหลดข้อมูล"

---

## 5️⃣ Testing

### Test Checklist:

#### ✅ Apps Script Tests:
- [ ] `setup()` ผ่าน - เข้าถึง Sheet ได้ทั้ง 2
- [ ] `testAPI()` ผ่าน - ดึงข้อมูลได้
- [ ] Web App deployed สำเร็จ
- [ ] Test URL ใน browser:
  ```
  YOUR_WEB_APP_URL?date=2026-02-09&shift=A
  ```

#### ✅ Dashboard Tests:
- [ ] เปิด dashboard.html ได้
- [ ] โหลดข้อมูลกะ A ได้
- [ ] โหลดข้อมูลกะ B ได้
- [ ] แสดงตารางข้อมูลถูกต้อง
- [ ] แสดง summary stats ถูกต้อง
- [ ] ไม่มี error ใน Console (F12)

#### ✅ Mobile Tests:
- [ ] เปิดบนมือถือได้
- [ ] Responsive design ทำงาน
- [ ] ปุ่มกดได้ง่าย

---

## 6️⃣ Troubleshooting

### ❌ "Cannot access Sheet A/B"

**สาเหตุ:**
- Sheet ID ผิด
- ไม่มีสิทธิ์เข้าถึง

**แก้ไข:**
1. ตรวจสอบ Sheet ID ใน `Code.gs`
2. ตรวจสอบ permissions ของ Sheet
3. Run `setup()` ใหม่

### ❌ "Missing required parameters"

**สาเหตุ:**
- API URL ใน config.js ผิด
- ส่ง parameters ไม่ครบ

**แก้ไข:**
1. ตรวจสอบ `config.js` → `API_URL`
2. ตรวจสอบ Console (F12) ดู request ที่ส่งไป

### ❌ "CORS Error"

**สาเหตุ:**
- Apps Script ไม่ได้ Deploy เป็น Web App
- ตั้งค่า "Who has access" ไม่ถูกต้อง

**แก้ไข:**
1. Re-deploy Web App
2. ตั้งค่า "Who has access" = **Anyone**

### ❌ ไม่มีข้อมูลแสดง

**สาเหตุ:**
- วันที่ที่เลือกไม่มีข้อมูลใน Sheet
- Column mapping ไม่ตรง
- รูปแบบวันที่ใน Sheet ไม่ตรงกับที่ API คาดหวัง

**แก้ไข:**
1. ตรวจสอบข้อมูลใน Sheet
2. เปิด `CONFIG.DEBUG = true` ใน Code.gs
3. ดู Execution log ใน Apps Script
4. ตรวจสอบ `CONFIG.COLUMNS` ว่าตรงกับ Sheet

### ❌ "Script error" หรือ timeout

**สาเหตุ:**
- Sheet มีข้อมูลมากเกินไป
- Code มีปัญหา

**แก้ไข:**
1. ลดจำนวนข้อมูลในการทดสอบ
2. ตรวจสอบ log หา error
3. ใช้ `testAPI()` debug

---

## 7️⃣ การ Update และ Maintenance

### Update Code:
1. แก้ไขโค้ดใน Apps Script Editor
2. **Save** (Ctrl+S)
3. **Deploy** → **Manage deployments**
4. คลิก Edit (ไอคอนดินสอ)
5. เปลี่ยน **Version** → **New version**
6. คลิก **Deploy**
7. URL จะเหมือนเดิม (ไม่ต้องแก้ config.js)

### Backup:
- Export ไฟล์ `Code.gs` เป็น backup
- Export `config.js` เก็บไว้
- Screenshot การตั้งค่า deployment

---

## 8️⃣ Security Best Practices

### ⚠️ สำคัญ:
1. **ไม่แชร์ Web App URL สาธารณะ** - ใครมี URL ก็เข้าถึงข้อมูลได้
2. **ตรวจสอบ Sheet permissions** - ให้สิทธิ์แค่คนที่จำเป็น
3. **ใช้ Apps Script service account** - สำหรับ production
4. **Enable logging** - เพื่อ audit trail

### Production Deployment:
- ตั้งค่า "Who has access" เป็น **Only myself** แทน **Anyone**
- ใช้ Authentication token
- ตั้ง rate limiting

---

## 📞 Support

หากมีปัญหา:
1. เปิด `CONFIG.DEBUG = true` ดู log
2. Run `testAPI()` ใน Apps Script
3. ตรวจสอบ Console (F12) ใน browser
4. อ่าน Troubleshooting ด้านบน

---

## ✅ Deployment Checklist

ก่อน Go Live:

- [ ] Apps Script deployed และทดสอบแล้ว
- [ ] config.js มี API URL ถูกต้อง
- [ ] ทดสอบบน browser หลายตัว
- [ ] ทดสอบบนมือถือ
- [ ] ทดสอบกับข้อมูลจริง
- [ ] มี backup ไฟล์ทั้งหมด
- [ ] เอกสารครบถ้วน

---

**🎉 เสร็จสมบูรณ์! ระบบพร้อมใช้งาน**
